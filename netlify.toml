[build]
  command = "npm run build"
  publish = "dist"

# Vite is pinned to port 5180 (vite.config.js / package.json),
# so point `netlify dev` at it instead of Vite's default 5173.
[dev]
  targetPort = 5180
