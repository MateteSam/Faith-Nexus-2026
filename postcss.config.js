export default {
  plugins: {
    // Tailwind's PostCSS plugin has moved to a separate package. Use the new package
    // name here and install `@tailwindcss/postcss` as a dev dependency.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
